import styled from "styled-components";
import Image from '../../images/horses.jpg';

export const BackgroundImageSection = styled.div`
  background: url('${Image}') no-repeat center center/cover;
  height: 60vh;
  background-attachment: fixed;
`; 